.root {
  display: grid;
  grid-template-columns: 14rem 1fr;
  grid-column-gap: 1rem;
  line-height: 2rem;
  color: var(--greyDark);
  letter-spacing: 0;
  align-items: center;

  strong {
    font-weight: var(--medium);
  }
}

.BeneficiaryAddress {}
