import styled from 'styled-components'
import { COLORS, MEDIUM } from '$shared/utils/styled'

export const NetworkSectionTitle = styled.h2`
    font-size: 20px;
    color: ${COLORS.primary};
    line-height: 30px;
    font-weight: ${MEDIUM};
    margin: 0;
`
