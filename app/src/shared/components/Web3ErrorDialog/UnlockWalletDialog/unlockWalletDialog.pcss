.addressWrapper {
  margin-top: 0.5rem;
}

.icon {
  margin-bottom: 0.75rem;
}

.address {
  background: #EFEFEF;
  border-radius: 4px;
  padding: 4px 8px;
  font-weight: var(--medium);
}

.content {
  padding: 40px;
}
