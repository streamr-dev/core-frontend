.select {
  font-size: 0.875rem;
}

.paddedSelect {
  top: 0.5rem;
}

.tick {
  width: 10px;
  height: 8px;
  position: absolute;
  top: 50%;
  left: 1.1rem;
  transform: translate(-50%, -50%);
}

.caret {
  width: 10px;
  height: 8px;
}
