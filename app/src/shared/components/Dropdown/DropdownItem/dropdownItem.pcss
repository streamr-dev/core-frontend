.dropdownItem {
  display: flex;
  align-items: center;
  font-size: 14px;
  cursor: pointer;
  margin-right: 10px;
  position: relative;
}

.tickIcon {
  width: 10px;
  position: absolute;
  right: 10px;
  top: 50%;
  transform: translateY(-50%);
}
