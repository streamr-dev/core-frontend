import { Link } from 'react-router-dom'

import links from '$shared/../links'

## Get building

Here's some ideas about what you can start building on the Streamr Platform:

#### Try one of our tutorials:
- <Link to={`${links.docs.tutorials}#building-a-simple-pub-sub-system`}>Building a simple pub/sub system</Link>
- <Link to={`${links.docs.tutorials}#building-custom-canvas-module`}>Building a custom canvas module</Link>

#### Check out the Core App to create, process, visualise and sell your realtime data:
- <Link to={links.docs.core}>Intro to Core</Link>

#### Create a product on the Data Marketplace:
- <Link to={links.docs.products}>Intro to Products</Link>

#### Leverage a Streamr SDKs and build apps powered by Streamr:
- <Link to={links.docs.SDKs}>SDKs overview</Link>

#### Get to know the Streamr API:
- <Link to={links.docs.api}>API overview</Link>
