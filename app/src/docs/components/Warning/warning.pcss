.root {
  position: relative;
  display: flex;
  flex-direction: column;
  border-radius: 4px;
  padding: 2rem;
  margin: 4rem 0;
  background: rgba(3, 36, 255, 0.05);
  border: 1px solid var(--streamrBlue);

  .warningIcon {
    position: absolute;
    top: 2rem;
    left: 2rem;
    width: 24px;
    height: 24px;
  }

  p {
    flex: 1;
    margin-left: 4rem;

    &:last-of-type {
      margin-bottom: 0;
    }
  }
}

@media (--xs) {
  .root {
    p {
      margin-left: 3rem;
    }
  }
}
