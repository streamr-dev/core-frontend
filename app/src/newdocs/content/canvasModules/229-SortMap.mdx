### SortMap

Sorts a map.

**Inputs**
**in**
a map


**Outputs**
**out**
a sorted map


**Parameters**
**byValue**
when false (default), sorts by key. when true, sorts by value


export const moduleConfig = {
  "id": 229,
  "name": "SortMap",
  "path": "Map",
  "help": {
    "params": {
      "byValue": "when false (default), sorts by key. when true, sorts by value"
    },
    "paramNames": [
      "byValue"
    ],
    "inputs": {
      "in": "a map"
    },
    "inputNames": [
      "in"
    ],
    "outputs": {
      "out": "a sorted map"
    },
    "outputNames": [
      "out"
    ],
    "helpText": "<p>Sorts a map.</p>"
  }
}