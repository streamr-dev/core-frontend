### GreaterThan

Outputs 1 if <span class="highlight">A</span> is greater than <span class="highlight">B</span> (A>B). If <span class="highlight">equality</span> is set to true, outputs 1 if A≥B. Otherwise outputs 0.










export const moduleConfig = {
  "id": 46,
  "name": "GreaterThan",
  "path": "Boolean",
  "help": {
    "outputNames": [],
    "inputs": {},
    "helpText": "<p>Outputs 1 if <span class='highlight'>A</span> is greater than <span class='highlight'>B</span> (A&gt;B). If <span class='highlight'>equality</span> is set to true, outputs 1 if A&ge;B. Otherwise outputs 0.</p>",
    "inputNames": [],
    "params": {},
    "outputs": {},
    "paramNames": []
  }
}