### HeadMap

Retrieve first (n=limit) entries of a map.

**Inputs**
**in**
a map


**Outputs**
**out**
a submap of the first entries of map


**Parameters**
**limit**
the number of entries to fetch


export const moduleConfig = {
  "id": 226,
  "name": "HeadMap",
  "path": "Map",
  "help": {
    "params": {
      "limit": "the number of entries to fetch"
    },
    "paramNames": [
      "limit"
    ],
    "inputs": {
      "in": "a map"
    },
    "inputNames": [
      "in"
    ],
    "outputs": {
      "out": "a submap of the first entries of map"
    },
    "outputNames": [
      "out"
    ],
    "helpText": "<p>Retrieve&nbsp;first (n=limit)&nbsp;entries of a map.</p>"
  }
}