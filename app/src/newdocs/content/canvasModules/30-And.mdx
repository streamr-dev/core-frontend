### And

Implements the boolean AND operation: outputs 1 if <span class="highlight">both</span> inputs equal 1, otherwise outputs 0.










export const moduleConfig = {
  "id": 30,
  "name": "And",
  "path": "Boolean",
  "help": {
    "outputNames": [],
    "inputs": {},
    "helpText": "<p>Implements the boolean AND operation: outputs 1 if <span class='highlight'>both</span> inputs equal 1, otherwise outputs 0.</p>",
    "inputNames": [],
    "params": {},
    "outputs": {},
    "paramNames": []
  }
}