### Variance

Calculates the bias-corrected sample variance (with N-1 in the denominator). Use the <span class="highlight">PopulationVariance</span> module for the non-bias-corrected population variance.










export const moduleConfig = {
  "id": 155,
  "name": "Variance",
  "path": "Time Series: Statistics",
  "help": {
    "outputNames": [],
    "inputs": {},
    "helpText": "<p>Calculates the bias-corrected sample variance (with N-1 in the denominator). Use the <span class='highlight'>PopulationVariance</span> module for the non-bias-corrected population variance.</p>",
    "inputNames": [],
    "params": {},
    "outputs": {},
    "paramNames": []
  }
}