### Or

Implements the boolean OR operation: outputs 1 if <span class="highlight">at least one</span> of the inputs equal 1, otherwise outputs 0.










export const moduleConfig = {
  "id": 31,
  "name": "Or",
  "path": "Boolean",
  "help": {
    "outputNames": [],
    "inputs": {},
    "helpText": "<p>Implements the boolean OR operation: outputs 1 if <span class='highlight'>at least one</span> of the inputs equal 1, otherwise outputs 0.</p>",
    "inputNames": [],
    "params": {},
    "outputs": {},
    "paramNames": []
  }
}