### SumOfSquares

Calculates the sum of squared input values in a sliding window of length <span class="highlight">windowLength</span>. At least <span class="highlight">minSamples</span> values are collected before producing output.










export const moduleConfig = {
  "id": 157,
  "name": "SumOfSquares",
  "path": "Time Series: Statistics",
  "help": {
    "outputNames": [],
    "inputs": {},
    "helpText": "<p>Calculates the sum of squared input values in a sliding window of length <span class='highlight'>windowLength</span>. At least <span class='highlight'>minSamples</span> values are collected before producing output.</p>",
    "inputNames": [],
    "params": {},
    "outputs": {},
    "paramNames": []
  }
}