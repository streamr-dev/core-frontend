### TailList

Retrieves the last (a maximum of limit) items of a list.

**Inputs**
**in**
input list


**Outputs**
**out**
a list containing the last items of a list


**Parameters**
**limit**
the maximum number of items to include


export const moduleConfig = {
  "id": 558,
  "name": "TailList",
  "path": "List",
  "help": {
    "params": {
      "limit": "the maximum number of items to include"
    },
    "paramNames": [
      "limit"
    ],
    "inputs": {
      "in": "input list"
    },
    "inputNames": [
      "in"
    ],
    "outputs": {
      "out": "a list containing the last items of a list"
    },
    "outputNames": [
      "out"
    ],
    "helpText": "<p><br />Retrieves the last&nbsp;(a maximum of limit) items of a list.</p>"
  }
}