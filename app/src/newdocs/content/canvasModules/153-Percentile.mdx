### Percentile

Calculates the value below which a given <span class="highlight">percentage</span> of values fall in a sliding window of observations.

**Inputs**
**in**
The input values


**Outputs**
**out**
The value under which <span class='highlight'>percentage</span> % of input values fall


**Parameters**
**minSamples**
Minimum number of observations for producing output
**windowLength**
Length of the sliding window
**percentage**
This percentage (0-100) of observations fall under the output of this module


export const moduleConfig = {
  "id": 153,
  "name": "Percentile",
  "path": "Time Series: Statistics",
  "help": {
    "outputNames": [
      "out"
    ],
    "inputs": {
      "in": "The input values"
    },
    "helpText": "<p>Calculates the value below which a given <span class='highlight'>percentage</span> of values fall in a sliding window of observations.</p>",
    "inputNames": [
      "in"
    ],
    "params": {
      "minSamples": "Minimum number of observations for producing output",
      "windowLength": "Length of the sliding window",
      "percentage": "This percentage (0-100) of observations fall under the output of this module"
    },
    "outputs": {
      "out": "The value under which <span class='highlight'>percentage</span> % of input values fall"
    },
    "paramNames": [
      "windowLength",
      "minSamples",
      "percentage"
    ]
  }
}