If the value at the <span class="highlight">if</span> input is 1, then outputs the value present at the <span class="highlight">then</span> input. Otherwise outputs the value at <span class="highlight">else</span> input.