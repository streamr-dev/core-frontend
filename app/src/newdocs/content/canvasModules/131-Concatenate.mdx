### Concatenate

Concatenates (appends) the given strings. For example if input <span class="highlight">A</span> is "foo" and input <span class="highlight">B</span> is "bar", the output is "foobar".










export const moduleConfig = {
  "id": 131,
  "name": "Concatenate",
  "path": "Text",
  "help": {
    "outputNames": [],
    "inputs": {},
    "helpText": "<p>Concatenates (appends) the given strings. For example if input <span class='highlight'>A</span> is \"foo\" and input <span class='highlight'>B</span> is \"bar\", the output is \"foobar\".</p>",
    "inputNames": [],
    "params": {},
    "outputs": {},
    "paramNames": []
  }
}