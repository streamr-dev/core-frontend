### RoundToStep

Rounds incoming values to given precision/step. The direction of rounding can be set with the <span class="highlight">mode</span> parameter.










export const moduleConfig = {
  "id": 120,
  "name": "RoundToStep",
  "path": "Time Series: Simple Math",
  "help": {
    "outputNames": [],
    "inputs": {},
    "helpText": "<p>Rounds incoming values to given precision/step. The direction of rounding can be set with the <span class='highlight'>mode</span> parameter.</p>",
    "inputNames": [],
    "params": {},
    "outputs": {},
    "paramNames": []
  }
}