### Equals

Outputs 1 if the inputs are equal within the specified <span class="highlight">tolerance</span>, that is, if abs(A-B) ≤ tolerance. Otherwise outputs 0.










export const moduleConfig = {
  "id": 45,
  "name": "Equals",
  "path": "Boolean",
  "help": {
    "outputNames": [],
    "inputs": {},
    "helpText": "<p>Outputs 1 if the inputs are equal within the specified <span class='highlight'>tolerance</span>, that is, if abs(A-B)&nbsp;&le;&nbsp;tolerance. Otherwise outputs 0.</p>",
    "inputNames": [],
    "params": {},
    "outputs": {},
    "paramNames": []
  }
}