Merges two event streams into one. Whatever arrives at inputs <span class="highlight">A</span> or <span class="highlight">B</span> is sent out from the single output. The inputs and the output can be connected to all types of endpoints. A runtime error may occur if there is a type conflict.