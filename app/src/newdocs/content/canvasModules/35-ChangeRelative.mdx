### ChangeRelative

Outputs the received value divided by the previous received value, or <span class="highlight">in(t) / in(t-1)</span>. If the previous received value is zero, the result is undefined and no output is produced.










export const moduleConfig = {
  "id": 35,
  "name": "ChangeRelative",
  "path": "Time Series: Simple Math",
  "help": {
    "outputNames": [],
    "inputs": {},
    "helpText": "<p>Outputs the received value divided by the previous received value, or <span class='highlight'>in(t)&nbsp;/&nbsp;in(t-1)</span>. If the previous received value is zero, the result is undefined and no output is produced.</p>",
    "inputNames": [],
    "params": {},
    "outputs": {},
    "paramNames": []
  }
}