### RemoveFromMap

Remove an entry for a map by key.

**Inputs**
**in**
a map
**key**
a key


**Outputs**
**out**
a map without the removed key





export const moduleConfig = {
  "id": 234,
  "name": "RemoveFromMap",
  "path": "Map",
  "help": {
    "params": {},
    "paramNames": [],
    "inputs": {
      "in": "a map",
      "key": "a key"
    },
    "inputNames": [
      "in",
      "key"
    ],
    "outputs": {
      "out": "a map without the removed key"
    },
    "outputNames": [
      "out"
    ],
    "helpText": "<p>Remove an entry for a map by key.</p>"
  }
}