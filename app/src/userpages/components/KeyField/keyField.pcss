.container {
  position: relative;
  max-width: 488px;
}

.actions {
  display: inline-block;
  position: absolute;
  right: 0;
  top: 50%;
  transform: translate(0, -50%);
  z-index: 1;
}

.withMenu {
  z-index: 2;
}

.permissionDropdown {
  position: absolute;
  bottom: 0;
}
