.root {}

.KeyField {}

.keyFieldContainer {
  position: relative;
}

.labelWrapper {
  display: flex;
  justify-content: space-between;
}

.keyNameHolder {
  left: 0;
  max-width: 100%;
  overflow: hidden;
  position: absolute;
  text-overflow: ellipsis;
  top: 0;
}

.label {
  flex-grow: 1;
  margin-right: 0.5um;
  position: relative;
}
