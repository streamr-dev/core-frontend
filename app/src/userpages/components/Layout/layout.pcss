.container {
  display: flex;
  flex-direction: column;
  flex-grow: 1;
}

.loadingIndicator {
  top: 2px;
}

.content {
  background-color: #EFEFEF;
  padding: 2.5rem 0;
  flex-grow: 1;
}

@media (--sm-down) {
  .content {
    padding: 0;
  }
}
