@import 'variables.pcss';

.profilePage {
  color: black;
  padding-bottom: 3em;
  background-color: white;
  z-index: 11;
  top: -4em;
  position: relative;
  font-size: 14px;

  @media (--md-up) {
    top: -5em;
  }
}
