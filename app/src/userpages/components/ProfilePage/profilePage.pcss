@import 'variables.pcss';

.profilePage {
  color: black;
  padding-bottom: 3em;
  position: relative;
  font-size: 14px;

  @media (--lg-up) {
    top: -7.4em;
    z-index: 11;
  }

  @media (--xs) {
    top: auto;
  }
}

.longText {
  line-height: 20px;
  color: #323232;
}
