.anonSelectControl.anonSelectControl {
  background-color: #FDFDFD;
  font-size: 14px;
}

.InputNewShare {
  display: grid;
  grid-template-columns: 1fr auto;
  grid-column-gap: 1um;

  .input {
    font-size: 14px;
    line-height: normal;
    background-color: #FDFDFD;
  }

  .button {
    height: 2.5um;
    width: 2.5um;
    padding: 0;
  }

  .plusIcon {
    height: 1um;
    width: 1um;
  }
}

.errorMessageWrapper {
  overflow: hidden;
}

.errorOverlay {
  pointer-events: none;
  transition: opacity 400ms ease;
  position: absolute;
  content: ' ';
  top: 0;
  left: 0;
  bottom: 0;
  right: 0;
  background: white;
  z-index: 0;
  opacity: 0;
}

.errorOverlay.errorOverlayVisible {
  pointer-events: auto;
  opacity: 0.5;
}
