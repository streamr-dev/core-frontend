.root {
  height: 100%;
  max-height: stretch;
  display: flex;
  flex-direction: column;
  font-size: 14px;

  .row {
    flex-shrink: 0;
  }

  .cell {
    padding: 1.4um 2um;
  }

  .userList {
    overflow: auto;
    flex-grow: 1;
    flex-shrink: initial;
    border-top: 1px solid #EFEFEF;
  }
}

.InputNewShare {
  display: grid;
  grid-template-columns: 1fr auto;
  grid-column-gap: 1um;

  label {
    grid-column: span 2;
  }

  .input {
    font-size: 14px;
  }

  .plusIcon {
    height: 1um;
    width: 1um;
  }
}

.userPermissions {
  padding: 1.4um 2um;

  &:not(:last-child) {
    border-bottom: 1px solid #EFEFEF;
  }

  > *:not(:last-child) {
    margin-bottom: 1.4um;
  }

  .permissionsHeader {
    display: grid;
    grid-template-columns: 1fr auto;
    align-items: center;

    h4 {
      font-size: inherit;
      margin: 0;
      line-height: normal;
      padding: 0;
    }

    .button {
      display: inline-flex;
      justify-content: center;
      padding: 0.5um;
      opacity: 0;
      transition: opacity 0.1s;
    }

    &:hover .button {
      opacity: 1;
    }

    .trashIcon {
      height: 1um;
      width: 1um;
    }
  }

  .permissionsCheckboxesWrapper {
    overflow: hidden;
    will-change: height;
  }

  .permissionsCheckboxes {
    display: grid;
    grid-template-columns: auto 1fr auto 1fr;
    grid-column-gap: 1um;
    align-items: center;

    .checkbox {
      margin-right: 0;
    }

    label {
      margin-bottom: 0;
    }
  }
}

.footer {
  border-top: 1px solid #EFEFEF;
  display: flex;

  :first-child {
    margin-right: auto;
  }
}
