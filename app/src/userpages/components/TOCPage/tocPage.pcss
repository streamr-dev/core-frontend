.pageTitle {
  color: var(--greyDark);
  font-size: 2.2rem;
  font-weight: var(--medium);
  letter-spacing: 0;
  line-height: 1.5rem;
  margin: 96px 0 48px;
}

.tocList {
  list-style-type: none;
  margin: 182px 4em 1em 0;
  padding: 0;
  text-align: right;
  position: sticky;
  top: 7em;
}

.tocListItem {
  color: var(--greyDark);
  font-size: 16px;
  letter-spacing: 0;
  margin-bottom: 0.8rem;
  line-height: 1.5rem;

  a {
    color: var(--greyDark);
    text-decoration: none;

    &:hover,
    &:active {
      color: var(--streamrBlue);
    }

    &.active {
      color: var(--streamrBlue);
    }
  }
}

@media (--md-down) {
  .tocList {
    margin: 12px 2em 1em 0;
    top: 1.5em;
  }

  .pageTitle {
    display: none;
  }
}

@media (--sm-down) {
  .tocList {
    display: none;
  }
}

@media (--md-down) {
  .hideTablet {
    display: none;
  }
}
