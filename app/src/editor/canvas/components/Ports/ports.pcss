.root {
  display: flex;
  font-family: var(--mono);
  font-size: calc(0.625 * var(--um));
  font-weight: var(--medium);
  justify-content: space-between;
  padding: calc(0.625 * var(--um)) 0;
}

.gutter {
  flex-shrink: 0;
  width: var(--um);
}

.ports,
.gutter {
  position: relative;
}
