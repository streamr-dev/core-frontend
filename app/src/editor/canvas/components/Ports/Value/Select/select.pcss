/* TODO(Mariusz): A lot is shared w/ Text. Share maybe? */

.root {
  height: var(--um);
}

.inner {
  position: relative;
}

.root select {
  appearance: none;
  background-color: transparent;
  border: 0;
  border-radius: calc(0.125 * var(--um));
  color: #ADADAD;
  font-weight: var(--medium);
  line-height: var(--um);
  outline: 0;
  padding: 0 calc(0.5 * var(--um));
  transition: 200ms linear;
  transition-property: background-color, color, box-shadow;
  width: auto;
}

.root select:hover,
.root select:focus {
  background-color: #F5F5F5;
}

.root select:focus {
  box-shadow: inset 0 0 0 1px #6AEDFF;
  color: #323232;
}

.control {
  position: absolute;
  width: 100%;
}

.spaceholder {
  visibility: hidden;
}
