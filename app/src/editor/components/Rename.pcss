.Rename {
  font-weight: bold;
  color: #525252;
  border: none;
  background: none;
  min-width: max-content; /* prevent canvas name being squashed out */
}

.Rename::selection {
  background: rgba(3, 36, 255, 0.1);
}

.RenameContainer:not(:focus-within) .Rename {
  pointer-events: none;
}
