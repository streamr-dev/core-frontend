/* TODO: Use rems. */

.root {
  background: none;
  border: 0;
  box-sizing: content-box;
  flex-shrink: 0;
  height: 24px;
  padding: 8px;
  width: 24px;
}

.root:--enter,
.root:--idle {
  outline: 0;
}

.root svg {
  display: block;
  opacity: 0.2;
}

.root:--enter svg {
  opacity: 0.62;
}
