// TODO: tl;dr Implement new Subscription tests.
//
// Recent changes to StreamrClient provider changed the way we think about the client. Tests in this
// file were marked as pending so instead of a refactor I simply emptied them. Let's write new ones
// when we're done with all them subscription timeouts.

describe('Subscription', () => {
    describe('create subscription', () => {
        xit('unsubscribes on isActive false', () => {})

        xit('unsubscribes on unmount', () => {})

        xit('onNoResend works', () => {})

        xit('can use resendLast values', () => {})

        xit('can use resendLast 0', () => {})
    })
})
