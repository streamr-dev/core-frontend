#!/usr/local/bin/expect -f
#
# This Expect script was generated by autoexpect on Fri Nov  1 11:43:17 2019
# Expect and autoexpect were both written by Don Libes, NIST.


set force_conservative 0  ;# set to 1 to force conservative mode even if
			  ;# script wasn't run conservatively originally
if {$force_conservative} {
	set send_slow {1 .1}
	proc send {ignore arg} {
		sleep .1
		exp_send -s -- $arg
	}
}

# Ensures we can connect to broker's websocket.
set timeout -1
spawn npx --quiet -- wscat -c http://localhost/api/v1/ws --slash
match_max 100000
expect -exact "\[2K\[3D\[32mConnected (press CTRL+C to quit)\[39m\r
\[1G\[0J> \[3G"
send -- "/close\r" # immediately closes connection once connected
expect eof
