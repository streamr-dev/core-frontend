@import '../variables.pcss';

@media (--sm-down) {
    :global {
        body.no-scroll {
            overflow: hidden;
            height: 100vh;
            position: fixed;
            width:100vw;
        }
    }
}
