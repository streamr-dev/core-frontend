import styled from 'styled-components'

export const IconButton = styled.button`
    appearance: none;
    background: none;
    border: 0;
    display: block;
    padding: 0;

    :active {
        transform: translateY(1px);
    }
`
